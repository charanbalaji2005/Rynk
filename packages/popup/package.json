{
  "name": "@rynk/popup",
  "version": "0.3.0",
  "description": "Rynk's small native 'Host this project' window (Tk / WinForms) with an in-terminal fallback.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "files": ["dist", "assets"],
  "scripts": { "build": "tsc -b", "dev": "tsc -b -w", "clean": "rm -rf dist *.tsbuildinfo" },
  "dependencies": { "@rynk/core": "workspace:*", "qrcode-terminal": "^0.12.0" },
  "license": "MIT"
}
